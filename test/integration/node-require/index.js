/* eslint-disable @typescript-eslint/no-var-requires */
const assert = require("assert");
const {MinQueue} = require("heapify/dist/heapify");
assert.ok(require("common").testFunction(new MinQueue()));
